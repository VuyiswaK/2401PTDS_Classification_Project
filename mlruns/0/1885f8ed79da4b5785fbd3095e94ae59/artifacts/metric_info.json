{
  "accuracy_score-2_X_train": "accuracy_score(y_true=y_train, y_pred=y_pred)",
  "accuracy_score-3_X_train": "accuracy_score(y_true=y_train, y_pred=y_pred)",
  "accuracy_score_X_train": "accuracy_score(y_true=y_train, y_pred=y_pred)",
  "f1_score-2_X_test": "f1_score(y_true=y_test, y_pred=y_pred_test, average='weighted', zero_division=0)",
  "f1_score-3_X_train": "f1_score(y_true=y_train, y_pred=y_pred, average='weighted', zero_division=0)",
  "f1_score-4_X_test": "f1_score(y_true=y_test, y_pred=y_pred_test, average='weighted', zero_division=0)",
  "f1_score-5_X_train": "f1_score(y_true=y_train, y_pred=y_pred, average='weighted', zero_division=0)",
  "f1_score-6_X_test": "f1_score(y_true=y_test, y_pred=y_pred_test, average='weighted', zero_division=0)",
  "f1_score_X_train": "f1_score(y_true=y_train, y_pred=y_pred, average='weighted', zero_division=0)",
  "precision_score-2_X_train": "precision_score(y_true=y_train, y_pred=y_pred, average='weighted', zero_division=0)",
  "precision_score-3_X_train": "precision_score(y_true=y_train, y_pred=y_pred, average='weighted', zero_division=0)",
  "precision_score_X_train": "precision_score(y_true=y_train, y_pred=y_pred, average='weighted', zero_division=0)",
  "recall_score-2_X_train": "recall_score(y_true=y_train, y_pred=y_pred, average='weighted', zero_division=0)",
  "recall_score-3_X_train": "recall_score(y_true=y_train, y_pred=y_pred, average='weighted', zero_division=0)",
  "recall_score_X_train": "recall_score(y_true=y_train, y_pred=y_pred, average='weighted', zero_division=0)"
}